Don't try to learn all the functions, just learn the categories.
Can modify from finished code instead of building from scratch.

- Dataloading (dataloaders, datasets)
    * Dataloaders.py
    * Datasets.py
- Transforms, pre-processing
- Model definition
- Loss functions
- Optimizers
- Training loop

Some useful functions for debugging:
- print("after ToTensor:", x.shape, x.dtype)  
    * e.g. torch.Size([3, 224, 224]) torch.float32
- If code looks too simple, it is probably inheriting some function like __call__

Important Facts
- To track tensor for gradient computation, turn requires_grad=True
- optimizer.zero_grad() clears out this tracking
- Hyperparameters are stuff like epoch_time, batch_size; parameters are within the model